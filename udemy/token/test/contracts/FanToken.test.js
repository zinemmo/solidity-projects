const FanToken = artifacts.require('FanToken');

contract('FanToken', (accounts) => {
  const [deployer, receiver, additional] = accounts;
  describe('when the contract is deployed', () => {
    it('should deploy the contract correctly', () => {});
  });
});
